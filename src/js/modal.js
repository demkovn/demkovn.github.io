/*Картинка в модальном окне (не работает)*/
 $('.modal_window').magnificPopup({type: 'image'
 });

/*Модальное окно*/
/*const modal = document.querySelector(".modalwindow");
const close = document.querySelector(".modalwindow-close");

setTimeout(() => { modal.classList.toggle("is-openmodalwindow") }, 10000)

function Modal() {
    close.addEventListener('click', () => {
        modal.classList.toggle("is-openmodalwindow")
    }
    )
}
Modal()*/